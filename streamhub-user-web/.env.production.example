# Public media frontend (streamhub-user-web) — Vercel Production env.
# Set this in the Vercel project's Environment Variables (Production).

# The public HTTPS URL of the backend API (the Caddy domain from docker-compose.deploy.yml).
NEXT_PUBLIC_API_BASE=https://streamhub-demo.duckdns.org
